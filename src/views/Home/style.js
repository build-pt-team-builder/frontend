import Styled from 'styled-components'
import {color, flex} from '../../components/DesignComponents/theme'

export default Styled.div`
    ${flex('row','normal','flex-start')};
    height: 100%;
    width: 100%;
    background-color: ${color.bg01};
`